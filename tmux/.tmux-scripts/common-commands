yarn test
yarn init -y
npm run test
npm run lint
npm run lint-fix
npm outdated
npx ncu -u
jest
npm i
git init
git status
git pull
go run .
date
docker compose build
docker compose up -d
docker compose ps -a
docker ps -a
docker stop (docker ps -aq)
docker rm (docker ps -aq)
kubectl get pod
kubectl exec -it (k-getpod $CURRENT_KUBE_CLUSTER api) -- bash
kubectl describe po/(k-getpod $CURRENT_KUBE_CLUSTER api) | grep -i image
kubectl describe po/(k-getpod $CURRENT_KUBE_CLUSTER translator) | grep -i image
k-logs $CURRENT_KUBE_CLUSTER 360 -f
k-logs $CURRENT_KUBE_CLUSTER translator -f
k-logs $CURRENT_KUBE_CLUSTER umi -f
k-logs $CURRENT_KUBE_CLUSTER agent-controller -f
k-logs $CURRENT_KUBE_CLUSTER api -f
cd /home/mrgeek/ansible-local && ansible-playbook -i hosts -K --tags neovim setup.yaml
clear && pnpm dev
clear && pnpm build
clear && pnpm install
